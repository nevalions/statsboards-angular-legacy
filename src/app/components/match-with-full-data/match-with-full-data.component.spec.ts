import { MatchWithFullDataComponent } from './match-with-full-data.component';

describe('MatchWithFullDataComponent', () => {
  describe('creation', () => {
    it('should create component class', () => {
      const component = new MatchWithFullDataComponent();
      expect(component).toBeTruthy();
    });
  });
});
