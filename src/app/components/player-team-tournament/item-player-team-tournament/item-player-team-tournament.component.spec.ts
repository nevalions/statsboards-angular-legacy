import { ItemPlayerTeamTournamentComponent } from './item-player-team-tournament.component';

describe('ItemPlayerTeamTournamentComponent', () => {
  describe('creation', () => {
    it('should create component class', () => {
      const component = new ItemPlayerTeamTournamentComponent();
      expect(component).toBeTruthy();
    });
  });
});
