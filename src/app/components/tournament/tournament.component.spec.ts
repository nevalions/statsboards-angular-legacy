import { TournamentComponent } from './tournament.component';

describe('TournamentComponent', () => {
  describe('creation', () => {
    it('should create component class', () => {
      const component = new TournamentComponent();
      expect(component).toBeTruthy();
    });
  });
});
