import { TeamTournamentComponent } from './team-tournament.component';

describe('TeamTournamentComponent', () => {
  describe('creation', () => {
    it('should create component class', () => {
      const component = new TeamTournamentComponent();
      expect(component).toBeTruthy();
    });
  });
});
